<% provide(:title, t("employee.job_applied.views.create.title")) %>

<div class="content">
  <div class="full-height">
    <div class="container">
      <div class="row justify-content-center">
        <div class="col-md-8 col-xxl-6">
          <div class="text-center mb-5">
            <%= t("employee.job_applied.views.create.desciption_html") %>
          </div>
          <div class="d-flex">
            <%= link_to t("employee.job_applied.views.create.chat"), chat_path(@room_chat.room_code), class: "btn btn-custom btn-primary me-2" %>
            <%= link_to t("employee.job_applied.views.create.back"), jobs_applied_index_path, class: "btn btn-custom btn-primary ms-2" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
