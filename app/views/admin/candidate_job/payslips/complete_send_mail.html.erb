<% content_for :title, "支払い明細メール送信完了" %>

<div class="container">
  <div class="row justify-content-center aligns-items-center mt-5">
    <div class="text-center mb-3 fw-bolder">
      <%= "支払い明細を送付しました。" %>
    </div>
    <div class="text-center send">
      <%= link_to admin_candidate_job_payslip_path(year: @year, month: @month) do %>
        <button type="button" class="btn save btn-submit"><%= "支払い明細へ" %></button>
      <% end %>
    </div>
  </div>
</div>
